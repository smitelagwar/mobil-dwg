# Android Validation V04 — Mimari ve gerçek Android uygulama kabuğu

Tarih: 2026-08-25

Durum: `VALIDATED`

Claim limit: `REAL_APP_SHELL_RUNTIME_ONLY_NOT_VIEWER_FIDELITY`.

Bu aşama gerçek `MobilDwg.App` Android APK'sının build/install/cold-launch/UI/liveness zincirini kanıtlar. DWG/DXF parser fidelity, scene/render fidelity ve fiziksel Android cihaz davranışı V04 kapsamında PASS değildir.

## Başlangıç auditi

V04 başlangıcında `src/MobilDwg.App/MobilDwg.App.csproj` yalnız `net10.0` hedefleyen platform-neutral bir projeydi. Core/Cad/Rendering composition ve safe-open kodu mevcuttu fakat:

- Android/MAUI target yoktu;
- installable APK yoktu;
- gerçek package ID/launcher yoktu;
- Android `MainActivity` / `MainApplication` yoktu;
- gerçek app UI root'u yoktu.

Dolayısıyla V01'de kullanılan geçici `Stage01Smoke` APK'sı gerçek ürün uygulaması değildi ve viewer sonucu olarak kullanılamazdı.

## Uygulanan V04 değişiklikleri

Mevcut dördüncü production proje korunarak `MobilDwg.App` Android-only MAUI executable'a dönüştürüldü; yeni beşinci production proje açılmadı.

Gerçek app sınırı:

- target: `net10.0-android36.0`;
- output: `Exe`;
- `UseMaui=true`, `SingleProject=true`;
- application ID: `com.smitelagwar.mobildwg`;
- min supported Android: API 24;
- `MainActivity` + `MainApplication` gerçek launcher/application sınıfları;
- sade shell UI marker'ları: `Mobil DWG` ve `Android app shell ready`;
- project references yalnız Core, Cad ve Rendering.

Dependency policy:

- `Microsoft.Maui.Controls` direct production dependency olarak CPM'e alındı;
- exact range: `[10.0.100]`;
- V02 production allowlist bu gerçek MAUI dependency'sini açıkça tanıyacak şekilde güncellendi;
- ACadSharp/SkiaSharp probe graph'i değiştirilmedi.

Architecture harness artık gerçek app için ayrıca `V04_REAL_ANDROID_APP_PROJECT_PASS` üretir. Core'un MAUI/ACadSharp/Skia bağımsızlığı ve mevcut Cad/Rendering dependency yönleri korunur.

## V04 runtime workflow'u

Workflow: `.github/workflows/android-v04-validation.yml` / `Android V04 Real App Shell`.

Self-hosted labels:

- `self-hosted`
- `windows`
- `android-test`
- `mobil-dwg`

Workflow iki ayrı kanıt katmanı kullanır:

1. hardened V01 infrastructure prerequisite: toolchain/emulator + executable harness + geçici Stage01Smoke; claim yalnız `INFRASTRUCTURE_SMOKE_ONLY`;
2. `scripts/v04-real-app-gate.ps1`: yalnız gerçek repository `MobilDwg.App` dependency/build/APK/install/launcher/PID/UI/screenshot/crash/ANR/liveness kanıtı.

Bu ayrım Stage01Smoke ile gerçek ürün APK'sının karıştırılmasını engeller.

## Gate geliştirme sırasında bulunan iki script hatası

İlk iki V04 workflow failure ürün build/crash failure değildi; gate'in kendi PowerShell hatalarıydı.

### Run 1

- run/job: `32830719039` / `97748547636`
- branch head: `7c7157c92c03589ade8cd61de81194dc59965f23`
- sonuç: workflow `FAIL`
- gerçek solution/MobilDwg.App Release build ve Core/Rendering/Architecture harness marker'ları geçti.
- failure: XML query sonucu tek nesne olduğunda PowerShell scalar üzerinde `.Count` kullanılması.

Düzeltme: sonuç explicit array ile normalize edildi; V01 prerequisite ve gerçek-app gate workflow'da ayrı steplere ayrıldı.

### Run 2

- run/job: `32831512720` / `97751032710`
- branch head: `1e7e79a6e074a6d255ee57a142abdc01c7dd89c8`
- sonuç: workflow `FAIL`
- V01 prerequisite tamamen PASS.
- gerçek `MobilDwg.App` Release build: `0 Warning(s)`, `0 Error(s)`.
- gerçek signed APK üretildi, `com.smitelagwar.mobildwg` olarak kuruldu ve launcher cold-launch `Status: ok` verdi.
- failure bundan sonra gate script'inde PowerShell'in read-only built-in `$PID` değişkenine yazılmaya çalışılmasıydı.

Düzeltme: gate değişkeni `$AppPid` olarak değiştirildi.

Bu iki failed run başarı kanıtı olarak kullanılmaz; yalnız gate hardening geçmişidir.

## Final authoritative V04 validation

Test edilen branch head:

`227ffa49c3095c4328f146acf1a2d9ecc07eb62d`

Test edilen PR synthetic merge revision:

`6201be929a636b963235f7da8ee72b0bbf9decf2`

Workflow checkout log'u exact olarak `6201be929a636b963235f7da8ee72b0bbf9decf2` revision'ını checkout etti.

Final run/job:

- run: `32832142832`
- job: `97752997848`
- runner: `DESKTOP-PKLGPNQ-mobil-dwg-runner`
- result: `SUCCESS`
- token permissions: `contents: read`, `metadata: read`

### Infrastructure / architecture prerequisite

- .NET SDK `10.0.400`: PASS
- `maui-android`: PASS
- OpenJDK `21.0.12`: PASS
- Android API 36 / Build-Tools 36.0.0 / ADB 37.0.1: PASS
- emulator: Google `sdk_gphone64_x86_64`, Android 16, API 36, ABI x86_64, QEMU=1
- solution Release build: `0 Warning(s)`, `0 Error(s)` for repository app/solution build
- `STAGE04_CORE_CONTRACT_TESTS_PASS`
- `STAGE04_RENDER_CONTRACT_TESTS_PASS`
- `STAGE09_RENDER_SCENE_TESTS_PASS`
- `STAGE04_ARCHITECTURE_TESTS_PASS`
- `STAGE05_DEPENDENCY_BOUNDARY_PASS`
- `V04_REAL_ANDROID_APP_PROJECT_PASS`
- V01 temporary smoke prerequisite: `ANDROID_EMULATOR_GATE_PASS`, claim `INFRASTRUCTURE_SMOKE_ONLY`

Temporary Stage01Smoke'daki historical deprecated-property warning yalnız V01 prerequisite generator'ına aittir; final repository `MobilDwg.App` build'i 0 warning/0 error'dır.

### MAUI direct dependency evidence

- `Microsoft.Maui.Controls`: exact `10.0.100` / CPM range `[10.0.100]`
- license: MIT
- nupkg SHA-256: `1cc7876e45fa5614fb84c80f53b1b07eb7f4f4b5fba0dba3b27aca6469f0757b`
- marker: `V04_MAUI_EXACT_LICENSE_PASS`

Aynı branch head üzerinde V02 dependency regression workflow'u da geçti:

- run/job: `32832142882` / `97752998222`
- result: `SUCCESS`

### Gerçek APK

- file: `com.smitelagwar.mobildwg-Signed.apk`
- size: `30,827,130` bytes
- SHA-256: `60d8d59b3fd452d786519a364875b155d3961c3e4aa210f986c004098789ba42`
- marker: `V04_REAL_APP_APK_PASS`

APK hash'i bu exact final build'e aittir; önceki runların APK hash'i authoritative final hash olarak kullanılmaz.

### Install / launcher / cold launch

- package: `com.smitelagwar.mobildwg`
- launcher: `com.smitelagwar.mobildwg/crc64d52a5cdc4f267319.MainActivity`
- `adb install`: `Success`
- marker: `V04_REAL_APP_INSTALL_PASS`
- cold launch: `Status: ok`, `LaunchState: COLD`
- final app PID: `3783`
- marker: `V04_REAL_APP_LAUNCH_PASS pid=3783`

### UI / screenshot / stability

UIAutomator hierarchy'de gerçek shell marker'ları bulundu:

- `Mobil DWG`
- `Android app shell ready`

Window manager evidence gerçek package ID'yi içerdi.

Marker'lar:

- `V04_REAL_APP_UI_PASS`
- `V04_REAL_APP_STABILITY_PASS`
- `ANDROID_VALIDATION_V04_PASS`

Ayrıca:

- screenshot byte-safe PNG olarak doğrulandı;
- package/PID-scoped crash buffer'da gerçek app crash'i saptanmadı;
- post-launch `am_anr` evidence saptanmadı;
- PID final kontrolde canlı kaldı.

## Final artifact

Artifact:

- name: `android-v04-real-app-evidence`
- ID: `9557331919`
- ZIP size: `29,878,096` bytes
- digest: `sha256:0ccdb5028b417212f6d428475e8793ebc9d3a8018164c63b2703228dda00c0b4`
- files uploaded: 20
- retention expiry: 2026-09-08

Artifact gerçek signed APK ile birlikte package graph, vulnerability evidence, direct MAUI package evidence, launch/UI/window, screenshot, logcat, crash/events, meminfo ve V01 prerequisite evidence'i taşır.

## Çıkış değerlendirmesi

V04 çıkış kriteri sağlandı:

- gerçek repository `MobilDwg.App` installable Android MAUI app'tir;
- mevcut Core/Cad/Rendering architecture boundary korunur;
- gerçek signed APK exact tested revision'da API 36 emulator üzerinde kurulur;
- gerçek launcher cold-launch olur;
- numeric PID + gerçek UI hierarchy + byte-safe PNG + crash/ANR/liveness evidence geçer;
- dependency exact-version/license boundary V02 ile regresyon görmez.

V04 `VALIDATED`.

Açık kalanlar:

- parser adapter'ın gerçek Android app içinde DWG/DXF smoke-set ile çalışması: V05;
- render/viewer engineering fidelity: V05+ / V09+ ve implementation stages;
- FilePicker/SAF: V06;
- fiziksel Android üretici/device farkı: `DEFERRED_RELEASE_DEVICE_GATE`.

Sonraki validation cursor: `V05 — ACadSharp parser entegrasyonu — NOT_STARTED`.

Bu V04 kapanış turunda V05 başlatılmaz. Implementation cursor `AŞAMA 10 — NOT_STARTED` olarak ayrı korunur.
