# Android Validation V05 — ACadSharp parser in real MobilDwg.App

## Result

`VALIDATED`

Claim limit: `REAL_ANDROID_APP_PARSER_SMOKE_ONLY_NOT_RENDER_FIDELITY`.

V05 proves that the production read-only `AcadSharpDocumentReader` path can parse the V03 redistributable DWG/DXF smoke inputs inside the real Android `MobilDwg.App` process on the API 36 emulator. It does not prove render/engineering fidelity, physical-device SAF behavior, or release readiness.

## Scope and invariants

- Active product remains Android-only, local/offline, read-only 2D DWG/DXF viewer.
- iOS remains `DEFERRED_FUTURE_OPTION`.
- Original CAD remains immutable.
- No writer/save/export path was added to production `src/`.
- Validation assets are included only when `V05Validation=true`; normal production app behavior remains the V04 shell behavior.
- Generated AC1015 DWG is validation-time writer/read-back smoke evidence, not an independent engineering-fidelity golden and not byte-deterministic across runs.
- Physical Android remains deferred to the release/device gate.

## Findings and fixes during V05

Two validation-gate portability defects were found before the authoritative PASS. Neither was a parser/product failure:

1. Historical `stage05-parser-spike.sh` used `/warnaserror`. Windows Git Bash/MSYS rewrote it as `C:/Program Files/Git/warnaserror`, causing MSBuild to reject the command line before parser validation. The switch was changed to portable `-warnaserror`.
2. The historical package gate parsed localized `dotnet list package` text with a grep pattern that did not accept the exact-range display `[3.7.1]`. The mini-corpus had already passed, but the localized output caused a false-negative. The final gate preserves the human-readable package graph as evidence and validates ACadSharp deterministically from `Directory.Packages.props`, `packages.lock.json`, and `project.assets.json` instead of localized CLI prose.

Final package marker:

`STAGE05_ACADSHARP_PACKAGE_PASS central=[3.7.1] resolved=3.7.1`

## Authoritative technical revision

- PR: `#18` — `feat(v05): validate ACadSharp parser in real Android app`
- final technical branch head: `d1552960d910b1fc6baea00ac14f6971344bd66e`
- main used by the successful PR synthetic merge: `b5b6a74ebcc9ea16eff4a423c3ff2e7cbb3e748c`
- exact tested PR synthetic merge revision: `3aa365dd92222ec445a589003fc796ee6290f505`
- workflow: `Android V05 Real App Parser`
- run: `32836712300`
- job: `97767085940`
- runner: `DESKTOP-PKLGPNQ-mobil-dwg-runner`
- conclusion: `SUCCESS`

The branch was subsequently fast-forwarded without force to the already-tested synthetic merge revision `3aa365dd92222ec445a589003fc796ee6290f505` so the parallel main documentation/protocol commit is preserved in ancestry before closure-document commits are added.

## Authoritative artifact

- artifact ID: `9559245377`
- name: `android-v05-real-app-parser-evidence`
- size: `29,657,586` bytes
- digest: `sha256:2453ac4df3b888c6235f240208b4674b834edc550dd1208ce37e34a6506d2b65`
- retention expiry recorded by GitHub: `2026-09-08`

## Host parser/corpus regression

Passed markers include:

- `V03_ANDROID_SMOKE_SET_PASS committed_positive=1 generated_positive=1 negative=2 formats=dwg,dxf`
- `STAGE03_FIXTURE_AUDIT_PASS fixtures=9 derived_negatives=2`
- `STAGE04_CORE_CONTRACT_TESTS_PASS`
- `STAGE04_RENDER_CONTRACT_TESTS_PASS`
- `STAGE09_RENDER_SCENE_TESTS_PASS`
- `STAGE04_ARCHITECTURE_TESTS_PASS`
- `STAGE05_DEPENDENCY_BOUNDARY_PASS`
- `V04_REAL_ANDROID_APP_PROJECT_PASS`
- `STAGE05_MINI_CORPUS_PASS fixtures=9 derived_negatives=2`
- `STAGE05_ACADSHARP_PACKAGE_PASS central=[3.7.1] resolved=3.7.1`
- `STAGE05_T3_PASS`
- `V05_HOST_PARSER_REGRESSION_PASS`

Positive host corpus covered pinned AC1015/AC1018/AC1024/AC1032 DWG, pinned AC1015/AC1032 ASCII DXF, the synthetic Turkish AC1015 DXF, and the missing-font/missing-XREF controlled fixtures.

Derived negative behavior was controlled:

- truncated AC1015 DWG → expected `EndOfStreamException` failure path
- corrupt AC1018 DWG → warning path

## Validation-time DWG

Markers:

- `STAGE03_SYNTHETIC_DWG_PACKAGE_PASS`
- `STAGE03_SYNTHETIC_DWG_GENERATED magic=AC1015 bytes=8021`
- `STAGE03_SYNTHETIC_DWG_READBACK_PASS`
- `V05_GENERATED_DWG_READY bytes=8021 sha256=44394883546bc115104be2dad50ba158abc0978d57439759d6d4273b88ac2122`
- `V05_PRODUCTION_WRITER_ABSENT_PASS`

The SHA-256 above is run-specific evidence only. The generated DWG container is not claimed byte-deterministic and is not an independent fidelity golden.

## Real Android app parser evidence

The validation build used the real repository app/package and the production reader path.

- Release validation APK: `30,876,566` bytes
- APK SHA-256: `a270689a6bda814b9145601498b075b8a3638dd03d6ed6d9026e293c5e0738b5`
- package: `com.smitelagwar.mobildwg`
- launcher: `com.smitelagwar.mobildwg/crc64d52a5cdc4f267319.MainActivity`
- cold launch: `Status: ok`
- cold launch TotalTime: `992 ms`
- validated app PID: `3803`

Final markers:

- `V05_REAL_APP_APK_PASS`
- `V05_REAL_APP_INSTALL_PASS`
- `V05_REAL_APP_UI_PARSE_PASS`
- `V05_REAL_APP_STABILITY_PASS pid=3803`
- `ANDROID_VALIDATION_V05_PASS`
- `CLAIM_LIMIT=REAL_ANDROID_APP_PARSER_SMOKE_ONLY_NOT_RENDER_FIDELITY`

## Same-head regressions

Normal production shell regression on the final technical head/synthetic-merge content also passed:

- workflow `Android V04 Real App Shell`
- run `32836712245`
- job `97767085274`
- conclusion `SUCCESS`
- V01 prerequisite and real `MobilDwg.App` API 36 runtime steps both passed.

Dependency regression also passed on the final technical head:

- workflow `Stage 02 Dependency Audit`
- run `32836712385`
- job `97767086999`
- conclusion `SUCCESS`
- artifact `9559261198`
- digest `sha256:e3d9dafeb576b20b63b06b96ba5b1729c15bece13f7d8426d0967d615841500a`

GitHub-hosted historical workflows that ended without an allocated runner are not used as V05 product PASS/FAIL evidence.

## Closure

V05 is `VALIDATED`. The next Android validation cursor is `V06 — NOT_STARTED` and must be opened in a later user turn only. V06 will cover the real-app FilePicker/SAF/safe-open/document-service bridge and lifecycle cases; none of those are claimed by V05.

The separate AŞAMA 10 workstream state remains governed by `docs/A10_WORKSTREAM.md`; V05 closure does not merge or complete AŞAMA 10.
