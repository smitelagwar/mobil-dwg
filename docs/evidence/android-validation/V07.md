# Android Validation V07 — ProCad NO-GO + production graph isolation + precision regression

## Result

`READY_FOR_RUNNER`

This is not a PASS. V07 remains open until the exact PR revision executes non-zero steps in the dedicated validation workflow and the required artifact/markers are verified.

Target claim limit if the gate passes:

`PROCAD_NO_GO_PRODUCTION_GRAPH_ISOLATION_AND_PRECISION_REGRESSION_ONLY`

## Scope

V07 revalidates the existing ADR 0002 rejection decision against the current Android production graph. It does **not** reopen the rejected ProCad candidate, upgrade it, patch it, install it on an emulator, or claim anything about a future precision-safe ProCad revision.

Required invariants:

- exact rejected ProCad source pin remains `f8a862b3e7634e27664fee02ff5d68774b102985`;
- ADR 0002 remains `Rejected / NO-GO` for the exact unpatched candidate;
- production `src/`, central package declarations, solution membership, lockfiles, resolved `project.assets.json` and built Android APK contain no ProCad/ProCadSharp dependency;
- the historical direct absolute `double -> float` survey-origin boundary still deterministically collapses `5,000,000 + 0.001` to zero observed delta;
- the current production double-precision camera/scene regression retains the 1 mm detail;
- ACadSharp `3.7.1` production parser baseline is not replaced by ProCad's historical fork/package graph;
- original CAD/read-only/writer-free product invariants remain unchanged;
- physical Android is not required for this deterministic dependency/precision decision and remains governed by later release/device gates.

## Starting revision

- base `main`: `8a875bedc848a8c690c1d6ee7ae40b47787dbf85`
- feature branch: `v07-procad-isolation-validation`
- historical decision evidence: `docs/evidence/STAGE_07.md`
- decision ADR: `docs/ADR/0002-procad-pinned-source-no-go.md`

## Validation design

Workflow: `Android V07 ProCad Isolation`.

The exact PR revision must execute:

1. V02 dependency/lockfile/license/native-boundary prerequisite;
2. exact ADR/source-pin consistency checks;
3. static production graph scan for ProCad/ProCadSharp;
4. locked restore plus resolved `project.assets.json`/lockfile scan;
5. real current `MobilDwg.App` Release build and APK entry scan;
6. deterministic reproduction of the rejected candidate's float-collapse boundary;
7. production rendering executable tests containing the survey-origin double-precision regression;
8. final `ANDROID_VALIDATION_V07_PASS` marker.

The workflow intentionally does not run `scripts/stage07-procad-source-spike.sh`, clone/build the rejected ProCad source, or install a ProCad smoke app on the emulator. Historical Stage07 source/build evidence is preserved; V07 is a current production-isolation/precision revalidation.

## Required final markers

- `V07_ADR_PIN_DECISION_PASS`
- `V07_STATIC_PRODUCTION_GRAPH_ISOLATION_PASS`
- `V07_RESOLVED_PRODUCTION_GRAPH_ISOLATION_PASS`
- `V07_APK_PROCAD_ABSENT_PASS`
- `V07_PROCAD_FLOAT_PRECISION_BLOCKER_REPRODUCED`
- `V07_PRODUCTION_DOUBLE_SCALAR_PASS`
- `V07_PRODUCTION_DOUBLE_PRECISION_REGRESSION_PASS`
- `V07_APP_PACKAGE_GRAPH_PROCAD_ABSENT_PASS`
- `ANDROID_VALIDATION_V07_PASS`
- `CLAIM_LIMIT=PROCAD_NO_GO_PRODUCTION_GRAPH_ISOLATION_AND_PRECISION_REGRESSION_ONLY`

## Current checkpoint

```text
V07: READY_FOR_RUNNER
PENDING_RESULT: Android V07 ProCad Isolation
PHYSICAL_ANDROID: DEFERRED_RELEASE_DEVICE_GATE
V08: SCOPE_ARCHIVED / ANDROID_GRAPH_CHECK_PENDING — NOT STARTED IN THIS TURN
A10: separate workstream; not advanced by this validation branch
```

Do not change this file to `VALIDATED` without a real non-zero-step PASS on the exact V07 PR revision.
