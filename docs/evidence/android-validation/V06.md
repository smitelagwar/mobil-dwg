# Android Validation V06 — real FilePicker/SAF + safe-open

## Result

`READY_FOR_EMULATOR`

This is not a PASS. V06 is open until the real `MobilDwg.App` API 36 workflow executes non-zero steps and produces the required evidence.

Target claim limit if the gate passes:

`REAL_ANDROID_APP_FILEPICKER_SAF_SAFE_OPEN_EMULATOR_ONLY_NOT_PHYSICAL_PROVIDER_FIDELITY`

## Scope

V06 validates the historical AŞAMA 06 safe-open implementation in the real Android app, not in the old generated Stage06 smoke app alone.

Required invariants:

- active product remains Android-only, local/offline, read-only 2D DWG/DXF viewer;
- original CAD remains immutable;
- production source contains no CAD writer/save/export path;
- `ACadSharp 3.7.1` remains the exact read-only parser baseline;
- FilePicker/SAF input is consumed through `FileResult.OpenReadAsync()` / stream semantics, not provider physical paths;
- selected content is immediately copied to app-private cache with byte quota, free-space reserve, unique atomic copy and deterministic cleanup;
- no broad external-storage permission is requested;
- no persistable URI grant is taken for this immediate private-copy flow;
- physical-device/provider-specific behavior remains `DEFERRED_RELEASE_DEVICE_GATE`.

## Starting revision

- base `main`: `8c2f986d66a3a795d3e886a4e8f3cb3607ed1978`
- feature branch: `v06-real-android-safe-open`
- implementation state: real app bridge coded; emulator evidence pending

## Production bridge under validation

The V06 branch wires the already-existing production safe-open components into the real `MobilDwg.App`:

- `src/MobilDwg.App/Opening/CadFileOpenContracts.cs`
- `src/MobilDwg.App/Opening/CadFileOpenCoordinator.cs`
- `src/MobilDwg.App/Opening/SafeCadFileCache.cs`
- `src/MobilDwg.App/Opening/MauiCadFilePickerAdapter.cs`
- `src/MobilDwg.App/MainPage.cs`

The real app now exposes DWG/DXF selection, cancel-request and close actions while keeping the V04 shell markers and V05 validation path available for regression testing.

## Required headless regression

`tools/Stage06.OpenFlowProbe` must pass on the exact V06 test revision with at least:

- `STAGE06_ACTUAL_DWG_DXF_PASS`
- `STAGE06_SAFE_COPY_GUARDS_PASS`
- `STAGE06_LAST_REQUEST_WINS_PASS`
- `STAGE06_CANCEL_SEMANTICS_PASS`
- `STAGE06_T2_HEADLESS_PASS`

These cover actual DWG/DXF private-copy parse, declared/actual byte quota, disk reserve, atomic cleanup, original immutability, last-request-wins and non-cooperative parser cancellation semantics.

## Required real Android evidence

Workflow: `Android V06 Real App Safe Open`.

The non-zero-step API 36 job must prove:

1. hardened V01 infrastructure prerequisite;
2. real `MobilDwg.App` build/install/cold launch;
3. real MAUI FilePicker launches Android DocumentsUI/SAF;
4. selected DWG opens through the production stream → private-copy → production parser path;
5. selected DXF opens through the same path;
6. a newer selection owns the current UI/session;
7. picker cancellation returns safely;
8. rotate preserves process/current state under the declared configuration-change model;
9. background/foreground preserves process/current state;
10. close disposes the session/private copy and reopen works;
11. externally selected CAD bytes remain unchanged;
12. no broad storage permission, package/PID-scoped crash or post-launch ANR;
13. final marker `ANDROID_VALIDATION_V06_PASS`.

## Current checkpoint

```text
V06: READY_FOR_EMULATOR
PENDING_RESULT: Android V06 Real App Safe Open
PHYSICAL_ANDROID: DEFERRED_RELEASE_DEVICE_GATE
V07: NOT_STARTED
A10: separate workstream; not started or merged by this validation branch
```

Do not change this file to `VALIDATED` unless the exact V06 workflow actually executes and the required evidence is verified.
